using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StageSE : MonoBehaviour
{
    public AudioSource objectVolume;

    // Start is called before the first frame update
    void Start()
    {
        objectVolume.Play();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
